# Negative controls and sanity checks (Section 5.5)
dataset: coco

cache_dir: data/cache
image_cache_file: image_feats_openai_clip-vit-base-patch32_raw.pt
text_cache_file:  text_feats_openai_clip-vit-base-patch32_raw.pt
output_dir: results/controls

vision_dim: 768
text_dim:   512
embed_dim:  256

jl_eps:  0.1
jl_seed: 42
jl_seeds:       [42, 43, 44, 45, 46]   # JL seed variability
training_seeds: [0, 1, 2]              # Training seed variability (separate axis)

epochs:        50
warmup_epochs: 10
batch_size:    4096
lr:            1.0e-3
temperature:   0.07
patience:      10
seed:          0

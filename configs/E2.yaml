# E2: Parameter efficiency — performance vs. trainable parameter count
dataset: coco
n_captions: 5

cache_dir: data/cache
image_cache_file: image_feats_openai_clip-vit-base-patch32.pt
text_cache_file:  text_feats_openai_clip-vit-base-patch32.pt
output_dir: results/E2

vision_dim: 512
text_dim:   512
embed_dim:  256      # Fixed m; vary rank

mahal_ranks: [null, 4, 16, 64]
lora_ranks:  [4, 16, 64]

jl_eps:  0.1
jl_seed: 42

epochs:      50
batch_size:  4096
lr:          1.0e-3
temperature: 0.07
patience:    5
seed:        0
